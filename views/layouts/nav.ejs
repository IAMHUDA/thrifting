<nav class="fixed top-0 left-0 w-full z-50 transition bg-transparent" id="navbar">
  <div class="mx-auto flex h-20 max-w-screen-xl items-center gap-8 px-4 sm:px-6 lg:px-8">
    <!-- Logo -->
    <a class="block" href="/">
      <span class="sr-only">Home</span>
      <img src="/images/logo-nobg.png" alt="Logo" class="h-12 w-auto">
    </a>

    <div class="flex flex-1 items-center justify-end md:justify-between">
      <!-- Navigation Links -->
      <nav aria-label="Global" class="hidden md:block">
        <ul class="flex items-center gap-8 text-sm font-chakra">
          <li>
            <a class="text-black text-2xl transition hover:text-teal-400" href="/"> Home </a>
          </li>
          <li>
            <a class="text-black text-2xl transition hover:text-teal-400" href="/product"> Product </a>
          </li>
          <li>
            <a class="text-black text-2xl transition hover:text-teal-400" href="/contact"> Contact </a>
          </li>
        </ul>
      </nav>

      <!-- Right Buttons -->
      <div class="flex items-center gap-4">
        <div class="sm:flex sm:gap-4">
          <% if (user) { %> <!-- Check if user is logged in -->
            <a href="/profile" class="block">
    <img src="/images/profile.jpg" alt="Profile" class="h-12 w-12 rounded-full" />
</a>

            <a
              class="block rounded-md bg-gray-200 px-6 py-3 text-sm font-chakra transition"
              style="color: #000000;"
              href="/logout"
            >
              Logout
            </a>
          <% } else { %> <!-- If not logged in -->
            <a
              class="block rounded-md bg-teal-500 px-6 py-3 text-sm font-chakra text-white transition hover:bg-teal-600"
              href="/login"
            >
              Login
            </a>
            <a
              class="hidden rounded-md bg-gray-200 px-6 py-3 text-sm font-chakra transition sm:block" 
              style="color: #000000;"
              href="/signup"
            >
              Register
            </a>
          <% } %>
        </div>

        <!-- Cart Icon -->
        <a href="/cart" class="relative block text-gray-600 hover:text-gray-800">
          <svg
              xmlns="http://www.w3.org/2000/svg"
              class="h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
          >
              <path stroke-linecap="round" stroke-linejoin="round" d="M3 3h2l1 7h14l1-7h2M7 16a3 3 0 100 6 3 3 0 000-6zm10 0a3 3 0 100 6 3 3 0 000-6z" />
          </svg>
          <span
              class="cart-badge absolute -top-2 -right-2 rounded-full bg-red-500 text-white text-xs font-bold w-5 h-5 flex items-center justify-center"
          >
              <%= totalItems || 0 %> <!-- Display total items in the badge -->
          </span>
        </a>
      </div>
    </div>
  </div>
</nav>
